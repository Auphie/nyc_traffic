# tests/

This directory will contain:

- pytest unit tests for Python ingestion helpers
- integration tests against temporary DuckDB databases
- fixture data used to validate incremental loading behavior

The first scaffold PR only creates the test home; actual tests land in later tickets.
